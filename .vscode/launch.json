{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Package",
      "type": "go",
      "request": "launch",
      "mode": "auto",
      "program": "${workspaceRoot}",
      "preLaunchTask": "Run Prelaunch Command"  // This should match the label from tasks.json
    }
  ]
}